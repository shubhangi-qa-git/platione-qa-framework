# Staging environment configuration
TEST_ENV=staging
BASE_URL=https://staging.plati-one.com
API_URL=https://staging.plati-one.com/api
TEST_USER=qa.user@plati-one.com
TEST_PASSWORD=changeme

# Staging hits the real backend, so disable mocks.
MOCK_API=false

DB_HOST=staging-db.plati-one.internal
DB_PORT=3306
DB_USER=qa_readonly
DB_PASSWORD=changeme
DB_NAME=platione_staging
